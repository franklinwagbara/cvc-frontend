import { FormatTimeAmPmPipe } from './format-time-am-pm.pipe';

describe('FormatTimeAmPmPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTimeAmPmPipe();
    expect(pipe).toBeTruthy();
  });
});
